//import java.util.NoSuchElementException;
//import java.util.Iterator;

public class LenkelisteIterator /* implements Iterator<T>*/ {
/*  protected Node posisjon;
  protected Lenkeliste lenkeliste;

  public LenkelisteIterator(Lenkeliste lenkeliste, Node posisjon) {
    this.lenkeliste = lenkeliste;
    this.posisjon = posisjon;
  }

@Override
  public boolean hasNext() {
    return posisjon.neste != null;
  }

@Override
  public T next() {
    if (!hasNext()) {
      throw new NoSuchElementException();
    }

    posisjon = posisjon.neste;
    T element = posisjon.hentElement();
    return element;
  }

@Override
  public void remove() {
    throw new UnsupportedOperationException(); //??
  } */
}
